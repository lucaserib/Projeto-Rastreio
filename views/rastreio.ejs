<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Rastreio PsMoveis</title>
    <link rel="icon" href="/favicon.png" type="image/x-icon" />
    <link
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      h1,
      p {
        font-weight: bold;
        color: #333;
        text-align: center;
      }
      .container {
        max-width: 500px;
        margin: auto;
      }
      .btn {
        display: block;
        width: 100%;
        margin-top: 20px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1 class="mt-5">Informações de Rastreio:</h1>
      <p class="mt-4">Código de Rastreio: <%= rastreio.codigoRastreio %></p>
      <p>
        Data de Recolha: <%= new
        Date(rastreio.dataRecolha).toLocaleDateString('pt-BR') %>
      </p>
      <p>
        Data de Previsão: <%= new
        Date(rastreio.dataPrevisao).toLocaleDateString('pt-BR') %>
      </p>
      <p>
        <%= rastreio.entregue ? 'Entregue: ' + new
        Date(rastreio.entregue).toLocaleDateString('pt-BR') : 'Em Transporte' %>
      </p>
      <% if (rastreio.inicioPrestacao) { %>
      <p class="mt-4">Início da Prestação: <%= rastreio.inicioPrestacao %></p>
      <% } %> <% if (rastreio.terminoPrestacao) { %>
      <p class="mt-4">Término da Prestação: <%= rastreio.terminoPrestacao %></p>
      <% } %>
      <a href="/" style="border-radius: 20px" class="btn btn-primary"
        >Fazer Nova Busca</a
      >
    </div>
    
  </body>
    <% if (error && error.length > 0) { %>
      <script>alert('<%= error %>');</script>
    <% } %>
</html>
